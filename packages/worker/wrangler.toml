name = "toaster-bulider-worker"
main = "src/index.js"
compatibility_date = "2024-11-21"
compatibility_flags = ["nodejs_compat"]
# Uncomment and set a custom domain route after first deploy or use "wrangler routes"
# routes = [
#   { pattern = "api.yourdomain.com", custom_domain = true }
# ]

[vars]
# Non-secret env vars go here
# Example: API_BASE="https://example.com"

# COURSE_SECRET_KEY will be set as a Secret via: wrangler secret put COURSE_SECRET_KEY
